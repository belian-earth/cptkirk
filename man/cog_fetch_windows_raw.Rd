% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{cog_fetch_windows_raw}
\alias{cog_fetch_windows_raw}
\title{Concurrently fetch one native-dtype window from each of several sources.}
\usage{
cog_fetch_windows_raw(
  srcs,
  level,
  xoff,
  yoff,
  xsize,
  ysize,
  bands,
  fill,
  io_concurrency
)
}
\description{
\code{srcs} and the per-tile \code{level}/\code{xoff}/\code{yoff}/\code{xsize}/\code{ysize} vectors are
parallel (one entry per tile). \code{bands} (1-based, empty = all) and \code{fill}
are shared across tiles. All sources are opened and their windows fetched
within a single runtime, so the tiles' network reads overlap. Returns a
list with one element per tile, each as in \code{cog_fetch_window_raw}.
}
