% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{cog_fetch_window_raw}
\alias{cog_fetch_window_raw}
\title{Fetch a pixel window as a NATIVE-dtype byte buffer (band-sequential,
native endianness) for staging a \verb{/vsimem} raster. \code{level}/\code{bands} as in
\code{cog_fetch_window}. Returns a list with \code{bytes} (raw vector), \code{xsize},
\code{ysize}, \code{n_bands}, \code{dtype} (GDAL type name), \code{bytes_per_sample}, and
\code{byte_order} ("LSB"/"MSB").}
\usage{
cog_fetch_window_raw(
  h,
  level,
  xoff,
  yoff,
  xsize,
  ysize,
  bands,
  fill,
  io_concurrency
)
}
\description{
Fetch a pixel window as a NATIVE-dtype byte buffer (band-sequential,
native endianness) for staging a \verb{/vsimem} raster. \code{level}/\code{bands} as in
\code{cog_fetch_window}. Returns a list with \code{bytes} (raw vector), \code{xsize},
\code{ysize}, \code{n_bands}, \code{dtype} (GDAL type name), \code{bytes_per_sample}, and
\code{byte_order} ("LSB"/"MSB").
}
