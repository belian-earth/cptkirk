% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{cog_fetch_window}
\alias{cog_fetch_window}
\title{Fetch a pixel window of an overview level from an open source. \code{level} is
1-based (1 = full resolution); \code{bands} is 1-based (empty = all). Returns a
list with \code{data} (band-sequential double vector), \code{xsize}, \code{ysize},
\code{n_bands}.}
\usage{
cog_fetch_window(
  h,
  level,
  xoff,
  yoff,
  xsize,
  ysize,
  bands,
  fill,
  io_concurrency
)
}
\description{
Fetch a pixel window of an overview level from an open source. \code{level} is
1-based (1 = full resolution); \code{bands} is 1-based (empty = all). Returns a
list with \code{data} (band-sequential double vector), \code{xsize}, \code{ysize},
\code{n_bands}.
}
